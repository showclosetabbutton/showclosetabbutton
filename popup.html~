<!DOCTYPE html>
<html lang="zh-TW">
<head>
  <meta charset="UTF-8">
  <!-- 加入 viewport meta 標籤，確保行動裝置上正確縮放 -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Button Manager</title>
  <link rel="stylesheet" href="popup.css">
</head>
<body>
  <div class="controls">
    <select id="button-select">
      <option value="close">Close Button</option>
      <option value="pocket">Pocket Button</option>
      <option value="home">Home Button</option>
      <option value="duplicate-tab">Duplicate Tab Button</option>
    </select>
    <button id="add-button">&#10133;</button>
  </div>
  <p id="message"></p>
  <ul id="button-list">
	  <!-- <li value='aa' draggable='true'>Close Button<span style='cursor:pointer'>❌</span></li>-->
  </ul>
  
  <div class="settings">
    <h3>The button's position on the web page</h3>
    <div class="radio-group">
      <label><input type="radio" name="position" value="top-left"> Top left</label>
      <label><input type="radio" name="position" value="top-right" checked> Top right</label>
      <label><input type="radio" name="position" value="bottom-left"> Bottom left</label>
      <label><input type="radio" name="position" value="bottom-right"> Bottom right</label>
    </div>
    <h3>Arrangement</h3>
    <div class="radio-group">
      <label><input type="radio" name="layout" value="vertical" checked> Vertical</label>
      <label><input type="radio" name="layout" value="horizontal"> Horizontal</label>
    </div>
    <button id="apply-button">APPLY</button>
  </div>
  
  <script src="popup.js"></script>
</body>
</html>

